import { Link } from 'react-router-dom';
import Icon from './Icon.jsx';
const fileLabel = (name='') => { const ext=name.split('.').pop()?.toUpperCase(); return ['PDF','DOC','DOCX','PPT','PPTX','PNG','JPG','JPEG'].includes(ext) ? ext : 'FILE'; };
export default function ResourceCard({ item }) { const type=fileLabel(item.file?.originalName); return <div className="col"><article className="resource-card"><div className={`resource-cover cover-${type.toLowerCase()}`}><div className="file-glyph"><Icon name="file" size={28}/><span>{type}</span></div><span className="resource-category">{item.Category?.name||'Học liệu'}</span></div><div className="resource-body"><div className="resource-subject">{item.Subject?.name||'Chưa phân loại'}</div><h3>{item.title}</h3><p className="line-clamp">{item.description}</p><div className="resource-meta"><span><Icon name="download" size={16}/>{item.downloadCount||0} lượt tải</span><span>{item.uploader?.fullName}</span></div><Link to={`/resources/${item.id}`} className="resource-link">Xem chi tiết <Icon name="arrow" size={17}/></Link></div></article></div>; }
