import { useState } from 'react';import { Link,useNavigate } from 'react-router-dom';import { useAuth } from '../contexts/AuthContext.jsx';import Icon from '../components/Icon.jsx';import { useToast } from '../contexts/ToastContext.jsx';
export default function Register(){const [form,setForm]=useState({fullName:'',email:'',password:'',confirm:''}),[show,setShow]=useState(false),[error,setError]=useState(''),[loading,setLoading]=useState(false),{register}=useAuth(),{notify}=useToast(),nav=useNavigate();const submit=async e=>{e.preventDefault();if(form.password!==form.confirm)return setError('Mật khẩu xác nhận chưa trùng khớp.');setLoading(true);setError('');try{await register({fullName:form.fullName,email:form.email,password:form.password});notify('Đăng ký thành công. Hãy đăng nhập!');nav('/login')}catch(e){setError(e.message)}finally{setLoading(false)}};return <div className="auth-page"><div className="auth-visual"><div className="auth-glow auth-glow-one"/><div className="auth-glow auth-glow-two"/><div className="auth-visual-content"><div className="brand-lockup auth-brand"><span className="brand-mark"><Icon name="book"/></span><span>KhIm Hub</span></div><span className="eyebrow light">Gia nhập cộng đồng</span><h1>Bắt đầu hành trình<br/>tri thức của bạn.</h1><p>Tạo tài khoản để khám phá, lưu trữ và chia sẻ những nguồn học liệu chất lượng.</p></div></div><div className="auth-form-wrap"><div className="auth-card"><div className="auth-heading"><span className="mobile-auth-brand">KhIm Hub</span><h2>Tạo tài khoản</h2><p>Chỉ mất một phút để bắt đầu.</p></div>{error&&<div className="alert alert-danger auth-alert">{error}</div>}<form onSubmit={submit}><label className="form-label">Họ và tên</label><input className="form-control" value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})} minLength="2" required/><label className="form-label mt-3">Email</label><input className="form-control" type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/><div className="row g-3"><div className="col-md-6"><label className="form-label mt-3">Mật khẩu</label><input className="form-control" type={show?'text':'password'} minLength="6" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/></div><div className="col-md-6"><label className="form-label mt-3">Xác nhận</label><input className="form-control" type={show?'text':'password'} value={form.confirm} onChange={e=>setForm({...form,confirm:e.target.value})} required/></div></div><label className="form-check my-3"><input className="form-check-input" type="checkbox" checked={show} onChange={()=>setShow(!show)}/> <span className="form-check-label">Hiện mật khẩu</span></label><button className="btn btn-primary btn-lg w-100" disabled={loading}>{loading?'Đang tạo tài khoản...':'Đăng ký'}</button></form><p className="switch-auth">Đã có tài khoản? <Link to="/login">Đăng nhập</Link></p></div></div></div>}

