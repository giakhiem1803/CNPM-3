import { useEffect,useState } from 'react';import { Link } from 'react-router-dom';import { api } from '../api/client.js';import Icon from '../components/Icon.jsx';
export default function History(){const [items,setItems]=useState([]),[error,setError]=useState('');useEffect(()=>{api.get('/history/downloads').then(r=>setItems(r.data.items)).catch(e=>setError(e.message))},[]);return <div className="container page-space"><div className="page-header"><div><span className="eyebrow">Hoạt động cá nhân</span><h1>Lịch sử tải xuống</h1><p>Xem lại những học liệu bạn đã sử dụng gần đây.</p></div><span className="header-icon"><Icon name="download"/></span></div>{error&&<div className="alert alert-danger">{error}</div>}{items.length?<section className="surface-card admin-section"><div className="table-responsive"><table className="table modern-table align-middle"><thead><tr><th>Tài liệu</th><th>Môn học</th><th>Thời gian tải</th><th></th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td><strong>{x.LearningResource?.title}</strong><small>{x.LearningResource?.file?.originalName}</small></td><td>{x.LearningResource?.Subject?.name}</td><td>{new Date(x.downloadedAt).toLocaleString('vi-VN')}</td><td><Link className="btn btn-sm btn-outline-primary" to={`/resources/${x.resourceId}`}>Mở lại</Link></td></tr>)}</tbody></table></div></section>:!error&&<div className="empty-state"><span className="empty-icon"><Icon name="download" size={34}/></span><h3>Chưa có lượt tải</h3><p>Các tài liệu bạn tải sẽ được ghi lại tại đây.</p><Link className="btn btn-primary" to="/">Khám phá học liệu</Link></div>}</div>}
