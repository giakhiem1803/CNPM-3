import { useEffect,useState } from 'react';import { api } from '../api/client.js';import ResourceCard from '../components/ResourceCard.jsx';import Icon from '../components/Icon.jsx';
export default function Favorites(){const [items,setItems]=useState([]),[error,setError]=useState('');useEffect(()=>{api.get('/favorites').then(r=>setItems(r.data.items)).catch(e=>setError(e.message))},[]);return <div className="container page-space"><div className="page-header"><div><span className="eyebrow">Bộ sưu tập cá nhân</span><h1>Học liệu yêu thích</h1><p>Lưu lại những tài nguyên hữu ích để truy cập nhanh bất cứ lúc nào.</p></div><span className="header-icon"><Icon name="heart" size={28}/></span></div>{error&&<div className="alert alert-danger">{error}</div>}<div className="row row-cols-1 row-cols-md-2 row-cols-xl-3 g-4">{items.map(x=><ResourceCard key={x.id} item={x}/>)}</div>{!items.length&&!error&&<div className="empty-state"><span className="empty-icon"><Icon name="heart" size={34}/></span><h3>Chưa có học liệu yêu thích</h3><p>Khi tìm thấy tài liệu hữu ích, hãy lưu lại để dễ dàng quay lại.</p><a className="btn btn-primary" href="/">Khám phá học liệu</a></div>}</div>}
